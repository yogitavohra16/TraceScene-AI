# Serves the built SPA and falls back to index.html for client-side routes
# (React Router) so a hard refresh on e.g. /cases/42 doesn't 404.
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}
